# Sample SDK Output

This folder contains a sample of the SDK headers auto-generated by the dumper when run against `Ravenswatch.exe` on **2026-05-16**.

The RVAs shown here are valid for that specific build. **Re-run the dumper against your current build to get fresh offsets.**

## Files

- `SDK.h` — Main entry point with pointer chain documentation
- `Offsets.h` — All VTables, Functions and Strings in one file
- `Player.h` — `BarkPlayerEntityCpnt` with position chain
- `Camera.h` — Camera + `SetupCameraShaderUniforms` (ViewProjMat)

## Engine identified

Custom proprietary engine using `oC*` naming convention. NOT Unreal, NOT Unity. Uses an ECS-like component pattern (`oCGameObject` + `EntityCpnt*` components).

## Networking stack

- Backend: **Stormancer** (cloud sessions)
- P2P: **Epic Online Services**
- Sockets: **RakNet**
- Auth: **Steam** + **EOS Connect**
